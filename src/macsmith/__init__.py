"""macsmith — offline MAC address and ARP/MAC-table tooling.

Consolidates eight single-purpose scripts into one library with two surfaces:
a pipeable CLI and a static browser app. Everything is a pure function from
text to text, so both surfaces run the same behavior and neither needs a
network connection.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
